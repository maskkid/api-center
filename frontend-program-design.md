# 项目设计

## 后端
app目录是已经完成的后端，fastapi的

## 前端
是一个微信小程序，使用tdesign，主要有首页、工具箱、我的，三个tab。主要是一个集成各种工具的app。所有工具都是独立的功能模块，功能完成后，写一个配置文件，配置所有模块的名称、模块标记、图标、路径（路径可以通过模块标记，自动拼接出来）、是否显示在首页、是否开启。在工具箱页面显示所有已开启的模块入口。首页显示配置为显示在首页的入口。点击后，进入对应模块的首页。可以给我生成所有模块的默认首页。

### 目录
代码生成到frontend-miniprogram

### 功能模块目前的内容
1. 文案库：进入模块后，主要是图文混合的文案列表，每个文案包含一段文字+多个图片。图片可以点开后预览、上下切换、放大等。文案上面有个一键下载按钮，调用小程序的下载接口，文字复制到剪贴板，图片调用保存到手机相册。
2. 文案支持从网页的链接传入文字和图片url列表，如果小程序不支持下载外部url，可以通过当前项目下 app中的后端接口，将url代理到当前接口域名下，组装成新的数据，交给文案详情页面渲染。实现1中的文案下载逻辑。

3. 其他功能模块，暂时不用实现，只需实现功能模块的入口，点击进入后，默认一个首页，显示模块名称即可。后面再实现。
